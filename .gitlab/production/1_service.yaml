apiVersion: v1
kind: Service
metadata:
  name: APP-NAME-service
  namespace: APP-NAMESPACE-PRD
spec:
  type: NodePort 
  selector:
    app: APP-NAME
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      name: APP-NAME
